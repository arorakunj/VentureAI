[build]
builder = "NIXPACKS"
buildCommand = "pip install -r ventureai/backend/requirements.txt"

[deploy]
startCommand = "uvicorn ventureai.backend.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
